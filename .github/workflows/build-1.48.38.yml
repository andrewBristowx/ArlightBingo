name: Compilar ArlightBingo 1.48.38

on:
  workflow_dispatch:
  push:
    branches:
      - build/1.48.38-fixed-mine-progressive-infection
    paths:
      - '.github/workflows/build-1.48.38.yml'
      - '.github/import/14838-fixed-mine-progressive-infection.patch.gz.b64.part*'
  pull_request:
    branches:
      - main
    paths:
      - '.github/workflows/build-1.48.38.yml'
      - '.github/import/14838-fixed-mine-progressive-infection.patch.gz.b64.part*'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 90

    steps:
      - name: Descargar repositorio
        uses: actions/checkout@v4
        with:
          ref: build/1.48.38-fixed-mine-progressive-infection

      - name: Configurar Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: maven

      - name: Aplicar corrección 1.48.38
        shell: bash
        run: |
          set -euo pipefail
          cat .github/import/14838-fixed-mine-progressive-infection.patch.gz.b64.part0* | base64 -d | gzip -d > /tmp/14838.patch
          if ! grep -q '<version>1.48.38</version>' pom.xml; then
            patch -p1 < /tmp/14838.patch
          fi
          python scripts/validate_1_48_38.py
          git diff --check
          grep -q '<version>1.48.38</version>' pom.xml
          grep -q 'version: 1.48.38' src/main/resources/plugin.yml
          grep -q 'detectLegacyMineSitesRobust' src/main/java/com/arlight/bingo/listeners/OverworldIslandLoreDecorator.java
          grep -q 'fixedMainMineSite' src/main/java/com/arlight/bingo/listeners/OverworldIslandLoreDecorator.java
          grep -q 'planRestoreMineSignatureSweep' src/main/java/com/arlight/bingo/listeners/OverworldIslandLoreDecorator.java
          grep -q 'planCleanVillageInfection' src/main/java/com/arlight/bingo/listeners/OverworldIslandLoreDecorator.java

      - name: Guardar fuente 1.48.38 en la rama
        if: github.event_name != 'pull_request'
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add pom.xml src/main/resources/plugin.yml BUILD_VERSION.txt SOURCE_VERSION.txt \
            CAMBIOS_1.48.38.txt PROBAR_1.48.38.txt scripts/validate_1_48_38.py \
            src/main/java/com/arlight/bingo/listeners/OverworldCampaignRecoveryCommands.java \
            src/main/java/com/arlight/bingo/listeners/OverworldIslandLoreDecorator.java
          if ! git diff --cached --quiet; then
            git commit -m 'ArlightBingo 1.48.38: reparar minas e infección musgosa progresiva'
            git push origin HEAD:build/1.48.38-fixed-mine-progressive-infection
          fi

      - name: Instalar ArlightCore 1.20.0
        shell: bash
        run: |
          set -euo pipefail
          git clone --depth 1 https://github.com/andrewBristowx/ArlightCore.git /tmp/ArlightCore
          mvn -B -U -DskipTests -f /tmp/ArlightCore/pom.xml clean install

      - name: Compilar con Maven
        shell: bash
        run: |
          set -euo pipefail
          mvn -B -U -DskipTests clean package

      - name: Verificar JAR
        shell: bash
        run: |
          set -euo pipefail
          JAR="$(find target -maxdepth 1 -type f -name '*.jar' ! -name '*-sources.jar' ! -name '*-javadoc.jar' ! -name 'original-*.jar' -print -quit)"
          test -n "$JAR"
          unzip -p "$JAR" plugin.yml | grep -q 'version: 1.48.38'
          jar tf "$JAR" | grep -q 'com/arlight/bingo/listeners/OverworldIslandLoreDecorator.class'

      - name: Reunir entregables
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p dist/source
          find target -maxdepth 1 -type f -name '*.jar' \
            ! -name '*-sources.jar' ! -name '*-javadoc.jar' ! -name 'original-*.jar' \
            -exec cp {} dist/ \;
          test -n "$(find dist -maxdepth 1 -type f -name '*.jar' -print -quit)"
          rsync -a \
            --exclude '.git/' \
            --exclude '.github/import/' \
            --exclude 'target/' \
            --exclude 'dist/' \
            ./ dist/source/
          (cd dist/source && zip -qr ../ArlightBingo-1.48.38-SOURCE.zip .)
          sha256sum dist/*.jar > dist/ArlightBingo-1.48.38-SHA256.txt
          ls -lh dist

      - name: Subir JAR compilado
        uses: actions/upload-artifact@v4
        with:
          name: ArlightBingo-1.48.38-JAR
          path: dist/*.jar
          if-no-files-found: error
          retention-days: 30

      - name: Subir fuente y verificación
        uses: actions/upload-artifact@v4
        with:
          name: ArlightBingo-1.48.38-SOURCE
          path: |
            dist/ArlightBingo-1.48.38-SOURCE.zip
            dist/ArlightBingo-1.48.38-SHA256.txt
          if-no-files-found: error
          retention-days: 30
