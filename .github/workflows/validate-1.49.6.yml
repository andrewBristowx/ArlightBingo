name: Validar ArlightBingo 1.49.6

on:
  pull_request:
    branches: [ main ]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: maven
      - name: Descargar fuente exacta 1.49.6
        run: |
          set -euo pipefail
          curl -L --fail --retry 3 'https://drive.usercontent.google.com/download?id=179_mm28mdM7maY6eyNZhL5zu2jcelIKI&export=download&confirm=t' -o /tmp/source.zip
          unzip -t /tmp/source.zip | tail -1
          mkdir -p /tmp/buildsrc
          unzip -q /tmp/source.zip -d /tmp/buildsrc
      - name: Instalar ArlightCore
        run: |
          set -euo pipefail
          git clone --depth 1 https://github.com/andrewBristowx/ArlightCore.git /tmp/ArlightCore
          mvn -B -U -DskipTests -f /tmp/ArlightCore/pom.xml clean install
      - name: Compilar
        working-directory: /tmp/buildsrc
        run: mvn -B -U -DskipTests clean package
      - name: Validar JAR
        run: |
          set -euo pipefail
          test -f /tmp/buildsrc/target/ArlightBingo-1.49.6.jar
          jar tf /tmp/buildsrc/target/ArlightBingo-1.49.6.jar >/dev/null
          unzip -p /tmp/buildsrc/target/ArlightBingo-1.49.6.jar plugin.yml | grep -F 'version: 1.49.6'
      - name: Subir JAR
        uses: actions/upload-artifact@v4
        with:
          name: ArlightBingo-1.49.6
          path: /tmp/buildsrc/target/ArlightBingo-1.49.6.jar
          if-no-files-found: error
          retention-days: 3
